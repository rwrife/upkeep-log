# Application boundary

Use cases and ports that coordinate domain behavior live here. Application code
may depend on `domain`, but it must not import Flutter widgets, platform plugins,
concrete adapters, or presentation code.
